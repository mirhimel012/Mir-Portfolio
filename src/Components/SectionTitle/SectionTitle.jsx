const SectionTitle = ({heading}) => {
    return (
        <div className="my-8">
            <h2 className="text-4xl text-center font-extrabold text-blue-800">{heading}</h2>
        </div>
    );
};

export default SectionTitle;